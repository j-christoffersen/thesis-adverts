config:
  target: "http://127.0.0.1:4000"
  phases:
  - duration: 30
    arrivalRate: 20
  payload:
    path: users.csv
    fields:
    - id
scenarios:
- flow:
  - get:
      url: "/adverts?userId={{ id }}"