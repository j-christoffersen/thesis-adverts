config:
  target: "http://127.0.0.1:4000"
  phases:
  - duration: 10
    arrivalRate: 1
  payload:
    path: users.csv
    fields:
    - id
scenarios:
- flow:
  - get:
      url: "/adverts?userId={{ id }}"