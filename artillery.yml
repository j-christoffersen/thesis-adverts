config:
  target: "http://127.0.0.1:4000"
  http:
    pools: 10
  phases:
  - duration: 30
    arrivalRate: 100
  processor: './artilleryGen.js'
scenarios:
- flow:
  - function: artilleryGen
  - get:
      url: "/adverts?userId={{ id }}"